package main

import (
	"fmt"
	"strconv"
)

func reverseNumber(n int) int {
	num := strconv.Itoa(n)
	digits := len(num)
	for i := (digits - 1); i > 0; i-- {

	}
}

func isPalindrome(n int) bool {

}

func main() {
	ans := 0
	fmt.Printf("Answer : %d\n", ans)
}
